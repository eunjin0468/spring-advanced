spring:
  application:
    name: expert

  datasource:
    url: jdbc:mysql://localhost:3306/EXPERT
    username: {$DATABASE_ID}
    password: {$DATABASE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret:
    key: zePDo5WneZdixbKbRR2ki00opXxp/mFTOb9w2CVxq34=